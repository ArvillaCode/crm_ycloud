/**
 * Coordinator for scheduled cron jobs and background tasks
 */
function initJobs() {
  console.log('[Jobs] Scheduled jobs initialized successfully (0 registered)');
}

module.exports = {
  initJobs,
};
